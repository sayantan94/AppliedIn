# Python
.venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
*.egg-info/
dist/
build/

# Node / CDK
node_modules/
cdk.out/
*.js.map

# Social
docs/social-x

# Secrets / local config (user-provided, never committed)
facts.seed.yaml
*.local.yaml
.env
# local runtime state: saved logins (secrets.json), your facts (facts.md),
# ADK sessions, artifacts, daemon pid/log — never commit
.local/

# OS / IDE
.DS_Store
.idea/

# résumé: local render + raw source
resume/preview.pdf

# your real résumé — keep it out of git
resume/base.tex
resume/*.pdf
output/
.gstack/

# drafts and images for posting about the project — personal, and 15MB of PNGs
docs/social-x/
